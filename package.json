{
  "name": "uptools",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "dev:worker": "wrangler dev --local",
    "dev:full": "start \"Wrangler\" cmd /k wrangler dev --local && vite",
    "prebuild": "node scripts/generate-seo.mjs",
    "build": "tsc && vite build --base=/",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250813.0",
    "@types/node": "^24.2.1",
    "typescript": "~5.8.3",
    "vite": "^7.1.0",
    "wrangler": "^4.30.0"
  }
}
